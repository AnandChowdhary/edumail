{
  "name": "edumail",
  "version": "0.9.1",
  "main": "index.js",
  "repository": "git@github.com:AnandChowdhary/edumail.git",
  "author": "Anand Chowdhary <mail@anandchowdhary.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build-run": "yarn build && node dist/index.js",
    "test": "jest --coverage --coverageReporters=text-lcov | coveralls",
    "test-without-reporting": "jest --coverage",
    "test-without-coverage": "jest",
    "postinstall": "mkdir -p dist && cd dist && git clone https://github.com/JetBrains/swot --depth 1 && cp -r swot ../swot"
  },
  "devDependencies": {
    "@types/fs-extra": "^7.0.0",
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "coveralls": "^3.0.3",
    "fs-extra": "^8.0.1",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "node-cache": "^4.2.0"
  },
  "jest": {
    "roots": [
      "<rootDir>"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
